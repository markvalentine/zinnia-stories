/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { StoryCardRoundComponent } from './story-card-round.component';

describe('Component: StoryCardRound', () => {
  it('should create an instance', () => {
    let component = new StoryCardRoundComponent();
    expect(component).toBeTruthy();
  });
});
