/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { StoryCardFullWidthComponent } from './story-card-full-width.component';

describe('Component: StoryCardFullWidth', () => {
  it('should create an instance', () => {
    let component = new StoryCardFullWidthComponent();
    expect(component).toBeTruthy();
  });
});
